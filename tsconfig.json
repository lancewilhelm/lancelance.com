{
  // Updated tsconfig to explicitly include local types (./types) and provide
  // stable module resolution even before Nuxt generates its internal config.
  //
  // Rationale:
  // 1. Diagnostics were failing to resolve '@vueuse/core' very early because
  //    the generated .nuxt/tsconfig.json (with Nuxt’s type augmentations) was absent.
  // 2. We add explicit include + path aliases so imports like "~/types/blog" can work
  //    without having to rewrite them to relative paths.
  // 3. We add the ./types directory (where shim & custom ambient types live) to ensure
  //    those declarations are always picked up.
  //
  // When Nuxt generates .nuxt/tsconfig.json it will still be extended (if present),
  // and our compilerOptions override or augment as needed.

  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    // General TS emit / module settings
    "target": "ESNext",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "allowJs": true,
    "jsx": "preserve",
    "strict": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "isolatedModules": true,
    "resolveJsonModule": true,

    // Project structure
    "baseUrl": ".",
    "paths": {
      "~/*": ["./*"],
      "@/*": ["./*"]
    },

    // Type roots (keep default @types plus our local ambient decls)
    "typeRoots": ["./types", "./node_modules/@types"]
  },

  // Files / directories to include in the program
  "include": [
    "app/**/*.ts",
    "app/**/*.tsx",
    "app/**/*.vue",
    "components/**/*.ts",
    "components/**/*.vue",
    "server/**/*.ts",
    "server/**/*.d.ts",
    "types/**/*.d.ts",
    "utils/**/*.ts",
    "pages/**/*.vue",
    "*.d.ts",
    "nuxt.config.ts"
  ],

  // Exclusions (node_modules is excluded by default, but list dist/.nuxt build outputs)
  "exclude": ["dist", ".output", ".nuxt", "node_modules"]
}
